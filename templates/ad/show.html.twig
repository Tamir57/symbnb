{% extends 'base.html.twig' %}

{% block title %}
    {{ad.title}}
{% endblock %}

{% block body %}

    {# <div class="adtitle" style="background-image: url(/symbnb/images/ad_cover_image.jpg)"> #}
    <div class="adtitle" style="background-image: url(https://picsum.photos/1000/350)">
    {# <div class="adtitle" style="background-image: url({{ad.coverImage}})">  #}
        <div class="container">
            <h1>{{ad.title}}</h1>
            <p>{{ad.introduction}}</p>
            <p class="h2"><strong>{{ ad.rooms }} chambres </strong> pour <strong> {{ad.price}}&euro; </strong> par nuit.</p>
            <a href="#" class="btn btn-primary">Réserver !</a>
        </div>
    </div>

    <div class="container">
        <div class="row">
            <div class="col-md-8 mb-3">
                {{ad.contenu |raw}}

                <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
                    <ol class="carousel-indicators">
                        {% for image in ad.images %}
                            <li data-target="#carouselExampleIndicators" data-slide-to="{{ loop.index0 }}" {% if loop.first %} class="active" {% endif %}></li>
                        {% endfor %}
                    </ol>
                    <div class="carousel-inner">
                        {% for image in ad.images %}
                        <div class="carousel-item {% if loop.first %} active {% endif %} ">
                            <img src="{{ image.url }}" alt="First slide" class="d-block w-100">    
                            <div class="carousel-caption d-none d-md-block">
                                <h5>{{ ad.title }}</h5>
                                <p>{{ image.caption }}</p>
                            </div>
                        </div>
                        {% endfor %}
                    </div>
                    <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                        <span class="sr-only">Previous</span>
                    </a>
                    <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                        <span class="carousel-control-next-icon" aria-hidden="true"></span>
                        <span class="sr-only">Next</span>
                    </a>
                </div>
            </div>
            <div class="col">
                <div class="row">
                    <div class="col-4">
                        <img src="http://placehold.it/64x64" alt="avatar julien">
                    </div>
                    <div class="col-8">
                        <h3>Julien Burgio</h3>
                        <span class="badge badge-primary">3 annonces</span>
                    </div>
                </div>
                <p>Lorem ipsum dolor, sit amet consectetur adipisicing elit. Repudiandae placeat corporis enim non necessitatibus. Cumque fugiat sint deserunt cupiditate dolorum.</p>
                <p>Doloribus accusamus corrupti ab vero? Consequatur tempore magni atque fugiat veritatis tempora voluptatem quidem, iure optio dolores at reprehenderit iusto.</p>
                <p>Iure, alias! Nulla commodi enim architecto totam repellendus quod eos unde ratione id obcaecati! Aliquam voluptas distinctio blanditiis autem libero?</p>
            </div>
        </div>
    </div>

{% endblock %}