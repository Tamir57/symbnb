{% extends 'base.html.twig' %}

{% block title %}Création d'une annonce{% endblock %}

{# Pour la partie _ad_images #}
{# Si le theme est dans la meme page on emploi _self
{% form_theme form _self %} #}
{% form_theme form 'ad/_collection.html.twig' %}

{% block body %}

<div class="container">
    <h1>Créer une nouvelle annonce</h1>

    {{ form_start(form) }}

    {# Test de personnalisation  #}
    {{ form_row(form.title,  {'attr': {'class': 'test' }}) }}

    {{ form_widget(form) }}
    
    <button type="submit" class="btn btn-primary">Créer la nouvelle annonce</button>

    {{ form_end(form) }}

    {# {{ form(form) }} #}
   
</div>

{% endblock %}


{% block javascripts %}
        <script src="/symbnb/public/js/ad.js"></script>
{% endblock %}